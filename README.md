# testkube

An image I used to test k8s, similar to the nginx bare image. This has middleware that exposes the hostname of the running container/pod.

You can use with docker: 

    docker push ssimpson/testkube:latest
